# Dependencies
node_modules/

# Build output
dist/

# Tool runtime artifacts
.chrome2safari-scratch/
*.staged/
staged_extension/
out/
build-output/

# Xcode artifacts
*.xcodeproj/
*.xcworkspace/
DerivedData/
*.appex
*.app/
# Root-only: don't catch the src/build/ source dir.
/build/
*.xcarchive
*.ipa
*.dSYM/
xcuserdata/
*.xcuserstate
*.build-log
*.log

# macOS
.DS_Store

# Local raw extension extracts
extension-extract/
*-extracted/

# Keep the source archive and guide tracked by default; uncomment to ignore
# claude-chrome.zip
# *.docx

# Generated conversion output
*_Safari/

# Local test-extension corpus (large binaries; not source)
test extensions/

# Editor / local MCP config
.vscode/

# Per-extension reports and project notes: kept local, not pushed.
# `test/` used to be listed here too, from back when the suite was local-only. It stopped
# being local the moment CI started gating publishes on `npm test`: an ignored suite means
# the gate runs whatever happens to be committed, which was 19 of 51 files.
reports/
docs/
CLAUDE.md
AGENTS.MD

# GitHub wiki clone (its own repo, pushed separately)
wiki/
